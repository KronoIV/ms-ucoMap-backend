# =============================================
# Configuracion Spring Boot -- UCO Campus Map
# =============================================

spring.application.name=ucomap-backend

# MongoDB: lee la URI desde variable de entorno; fallback a localhost para desarrollo
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://admin:eWxxjXuVWRRNRCqS@ucomap.02mi34n.mongodb.net/ucoCampusMap?appName=UcoMap}

# Puerto: Render/Railway inyectan $PORT automaticamente
server.port=${PORT:8080}

# Inicializacion de datos
# Cambia a "false" en produccion una vez sembrada la DB
app.data.init-on-startup=${INIT_DATA:true}

# CORS
app.cors.allowed-origins=${CORS_ORIGINS:*}

# Logging
logging.level.com.ucomap.backend=INFO
logging.level.org.springframework.data.mongodb=WARN
